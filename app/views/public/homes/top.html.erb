<div class="container">
  <div class="row m-5">
    <div class="col-lg-12 text-center">
      <p>
        ようこそながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品閲覧はできますが、<br>
        購入には会員登録が必要になります。
      </p>
    </div>
  </div>
  <div class="row">
    <h2>新着商品</h2>
    <div class="col-12 d-flex">
        <% @items.each do |item| %>
          <div class="list-group-item m-3">
            <%= link_to item_path(item.id) do %>
              <%= image_tag item.get_image(200,200) %>
            <% end %></br>
            <%= item.name %></br>
            ¥<%= item.price %></br>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row">
    <p class="col-lg-8 offset-3 text-right"><%= link_to "すべての商品を見る>", items_path, class: "p-3" %></p>
  </div>
</div>
