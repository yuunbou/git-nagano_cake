<div class="container">
  <div class="row">
    <h4 class="bg-light col-lg-3 text-center">ジャンル編集</h2>
  </div>
  <div class="row">
    <div class="col-lg-12">
      <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
        <table>
          <tr>
            <th>ジャンル名</th>
            <td><%= f.text_field :name %></td>
            <td><%= f.submit '変更を保存', class="btn btn-success" %></td>
          </tr>
        </table>
      <% end %>
    </div>
  </div>
</div>

<%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>

  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <%= f.submit '変更を保存' %>
<% end %>
